{
  "name": "itarea",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test && node --check dist/itarea-standalone.js && node --check dist/itarea.js",
    "build": "node scripts/build-dist.mjs"
  }
}
